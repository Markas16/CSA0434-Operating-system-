#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>

int main() {
    int fd;
    char buf[50];

    fd = open("file.txt", O_RDONLY);
    read(fd, buf, 50);

    printf("Content: %s\n", buf);

    close(fd);
    return 0;
}
