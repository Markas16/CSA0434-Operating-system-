#include <stdio.h>

int main() {
    int start, length;

    printf("Enter start block & length: ");
    scanf("%d%d",&start,&length);

    printf("Allocated blocks: ");
    for(int i=0;i<length;i++)
        printf("%d ", start+i);

    return 0;
}
