#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>

int main() {
    int fd;
    char buf[20];

    fd = open("file.txt", O_RDONLY);

    lseek(fd, 5, SEEK_SET);
    read(fd, buf, 10);

    buf[10] = '\0';
    printf("After lseek: %s\n", buf);

    close(fd);
    return 0;
}
